import { calculateFullTax } from "../tax/fullCalculator.js";

// Redefine if required, or export directly
export { calculateFullTax }; 

// Additional function to integrate `calculateFullTax`
export async function calculateUserTaxes(userId, state) {
  const session = await getUserSession(userId); // Fetch user session
  const result = calculateFullTax(session, state); // Use the imported fullTax logic
  return result;
}